name: volley_scout
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+11

environment:
  sdk: ^3.12.2

# Monorepo (pub workspace) — vedi docs/dati-stagionali.md, passo 5.
# La root è l'app mobile; i package sotto `packages/` sono i pezzi condivisi
# con la futura dashboard web. Un solo `flutter pub get` per tutti, un solo
# pubspec.lock: app e dashboard non possono risolvere versioni diverse della
# stessa dipendenza, che è il modo classico in cui due metà dello stesso
# progetto divergono senza accorgersene.
workspace:
  - packages/volley_stats

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # Package condiviso del monorepo (vedi workspace sopra): logica pura
  # riusabile dalla dashboard web.
  volley_stats: ^0.1.0
  flutter_localizations:
    sdk: flutter
  intl: any

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.2
  drift: ^2.34.0
  sqlite3_flutter_libs: ^0.6.0+eol
  path_provider: ^2.1.5
  path: ^1.9.1
  pdf: ^3.13.0
  printing: ^5.15.0
  shared_preferences: ^2.5.5
  csv: ^6.0.0
  share_plus: ^13.0.0
  package_info_plus: ^10.0.0
  url_launcher: ^6.3.1
  purchases_flutter: ^10.0.0
  file_selector: ^1.1.0
  archive: ^4.0.9
  xml: ^7.0.1
dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  drift_dev: ^2.34.0
  build_runner: ^2.15.0
  flutter_launcher_icons: ^0.14.4
  # Solo per test/data/migrazione_uid_test.dart: apre un DB sqlite in memoria
  # "a mano" per degradarlo a schema v18 e verificare la migrazione. Arriva
  # comunque con drift, ma un import diretto va dichiarato.
  sqlite3: ^3.5.0

# Icona app — generata con `dart run flutter_launcher_icons`. image_path è la
# versione appiattita su sfondo bianco (icona legacy/iOS, niente trasparenza);
# adaptive_icon_foreground è l'originale trasparente, composto sopra
# adaptive_icon_background (bianco) per le icone adattive Android 8+.
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"
  min_sdk_android: 21
  remove_alpha_ios: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Genera AppLocalizations dai file .arb in lib/l10n (vedi l10n.yaml).
  generate: true

  assets:
    - assets/images/
    - assets/demo/
    # Logo app (già usato per l'icona launcher): caricato a runtime anche
    # nell'header del report PDF (match_pdf_screen.dart).
    - assets/icon/
    # Icone disegnate a mano usate DENTRO l'interfaccia (ImageIcon): sagome
    # monocrome su fondo trasparente, la tinta la mette il widget. Cartella
    # separata da `assets/icon/`, che è tutt'altro: lì c'è il logo dell'app.
    - assets/ui_icons/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  fonts:
    - family: Barlow
      fonts:
        - asset: assets/fonts/Barlow/Barlow-Regular.ttf
          weight: 400
        - asset: assets/fonts/Barlow/Barlow-Medium.ttf
          weight: 500
        - asset: assets/fonts/Barlow/Barlow-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Barlow/Barlow-Bold.ttf
          weight: 700
